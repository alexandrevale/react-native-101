export default (state = null, action) => {
	// If 'undefined' state = null
	
	// console.log(action);
	
	switch (action.type) {
		case 'select_library':
			return action.payload;
		
		default: 
			// last state 
			return state; 
	}
};